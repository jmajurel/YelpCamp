<!DOCTYPE html>
<html>
  <head>
    <title>YelpCamp</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="icon" 
      type="image/png" 
      href="logo/appLogo">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Quicksand" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="/stylesheets/style.css"> 

  </head>
  <body>
    <nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">
	<img src="/logo/appLogo" width="30" height="30" class="d-inline-block align-top" alt="">
	YelpCamp
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#yelpCampNavBar" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
	<span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-between" id="yelpCampNavBar">
	<ul class="navbar-nav">
	  <li class="nav-item">
	    <a class="nav-link <%= typeof page !== 'undefined' && page === 'campgrounds' ? 'active' : '' %>" href="/campgrounds">Campgrounds</a>
	  </li>
	</ul>
	<ul class="navbar-nav">
          <% if(!currentUser) { %>
            <li class="nav-item">
              <a class="nav-link <%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>" href="/users">Sign In</a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>" href="/login">Login</a>
            </li>
          <% } else { %>
            <li class="nav-item">
	      <span class="navbar-text">Logged in as: 
                <a style="display: inline" class="nav-link" href="/users/<%= currentUser._id %>"><%= currentUser.username %></a>
                <% if(currentUser && currentUser.isAdmin){ %>
                   (admin)
                <% }; %>
              </span>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/logout">Logout</a>
            </li>
          <% }; %>
	</ul>
      </div>
    </nav>
    <div class="container">
      <% if(error && error.length > 0){ %>
        <div class="alert alert-danger" role="alert">
          <%= error %>
        </div>
      <% }; %>

      <% if(success && success.length > 0){ %>
        <div class="alert alert-success" role="alert">
          <%= success %>
        </div>
      <% }; %>
   </div>
   <div class="container">
